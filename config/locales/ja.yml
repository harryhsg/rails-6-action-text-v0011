ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            base:
              content_attachment_bute_size_is_too_big: 添付ファイルは%{max_content_attachment_mega_byte_size}MB以下にしてください。[%{bytes}/%{max_bytes}]
            title:
              blank: が空です。
              too_long: が%{count}文字を超えています。
            content:
              too_long: が%{max_content_length}文字を超えています。[%{length}/%{max_content_length}]
              content_attachments_count_is_too_many: 添付ファイル数は%{max_content_attachments_count}以下にしてください。[%{content_attachments_count}/%{max_content_attachments_count}]
    attributes:
      post:
        title: タイトル
        content: コンテンツ